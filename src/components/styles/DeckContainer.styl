.deck-container {
  display: flex;
  flex: 1;
  margin: 20px;
  padding: 20px;
  background-image: url('/background_image.jpg');
  position: relative;
}
/*
.deck-container::after {
  content: "";
  background-image: url('/background_image.jpg');
  opacity: 0.5;
  top: 0;
  left: 0;
  bottom: 0;
  right: 0;
  position: absolute;
  z-index: -1;
}*/